# プロジェクト規約・指針

## CLIの利用
- 本ディレクトリで `agy .` を実行することで、AIエージェントによる開発支援を開始できます。

## コミュニケーション
- 本プロジェクト内でのコミュニケーションおよびドキュメント作成は、すべて **日本語** で行ってください。

## ワークフロー
- 新しいセッションを開始する際は、必ず `README.md` を読み込み、プロジェクトの現状を把握してから作業を開始してください。
- リモートの `main` ブランチへの直接コミットは禁止です。
- すべての変更は以下の手順に従って行い、ユーザーの承認を経てマージするものとします：
    1.  作業ブランチ（`feature/` や `docs/` 等）を作成して作業。
    2.  プルリクエスト（PR）を作成し、ユーザーにレビューを依頼。
    3.  マージ完了後、最新の `main` をローカルに反映し、不要になった作業ブランチをローカルおよびリモートの両方から削除する。

## 開発・メンテナンス方針
- **テストの優先**: 新機能の追加やバグ修正、リファクタリングを行う際は、動作確認を徹底してください。
- **検証の徹底**: スクリプトの変更後は、手動での統合テスト（GASエディタからの実行等）を実施し、デグレ（先祖返り）が発生していないことを確認した上で完了とします。
- **今後の展望**: より高度な検証環境が必要になった場合は、`clasp` と `Jest` を組み合わせたローカルテスト環境の導入を検討します。
